appId: com.wanda.ticket.tool
productName: 万达快速出票
directories:
  output: dist
  buildResources: build
files:
  - out/**/*
  - package.json
asar: true
compression: maximum
electronLanguages:
  - zh-CN
  - en-US
win:
  target:
    - dir
  artifactName: ${productName}-${version}-${arch}.${ext}
